# Uso local (python manage.py runserver), fora do Docker.
# Em produção/Docker, essas variáveis vêm de Docker secrets via
# entrypoint.sh — nunca deste arquivo. Ver PRD_BACKEND_DJANGO.md, 5.3.
DEBUG=True
ALLOWED_HOSTS=*
DB_HOST=localhost
DB_PORT=5432
db_user=sga_user
db_password=troque_por_uma_senha_forte_local
db_name=sga_db
