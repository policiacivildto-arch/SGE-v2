# Database Connection (Default: local SQLite database)
DATABASE_URL=sqlite:///./sga_database.db
# For PostgreSQL: postgresql://user:password@localhost:5432/sga_db

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=True

# Email / SMTP Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=suporte@sga.se.gov.br
SMTP_PASSWORD=sua_senha_aqui
SMTP_FROM=no-reply@sga.se.gov.br

# Frontend URL (for confirmation links and CORS)
FRONTEND_URL=http://localhost:3000
